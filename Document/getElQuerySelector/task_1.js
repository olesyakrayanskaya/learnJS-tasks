let tableAgeTable = document.getElementById('age-table');
let lables = tableAgeTable.getElementsByTagName(label);
let firstTd = table.rows[0].cells[0];
let searcForm = document.querySelector('form[name="search"]');
let inputs = searcForm.querySelectorAll('input');
let firstInput = inputs[0];
let lastInput = inputs[inputs.length - 1];